//Bonus (Optionnel)

package functions

//Partie Connexion
//Les deux fonctions doivent renvoyer true sinon on peut pas se connecter
func IsUserExisting() {
	//Vérifier si l'utilisateur existe bien dans le json si lui dire ce pseudo n'existe pas
}

func IsGoodPassword() {
	//Vérifier si le mot de passe relié à tel utilisateur est bon sinon lui envoyez un message
}

//Partie Inscription
func IsValidUsername() {
	//Vérifier si le pseudo est bien entre 3 et 12 caractères valides
	//Vérifier si le pseudo et déjà prit dans le json
}

func IsValidPassword() {
	//Vérifier si le mot de passe fait 12 caractères min.
	//Vérifier s'il contient bien un caractère spécial, une majuscule, une minuscule et un chiffre
	//Vérifier s'il l'utilisateur s'en souvient en lui faisant taper 2 fois
}
